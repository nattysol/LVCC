{
  "name": "lv-chocolate-backend",
  "version": "0.0.1",
  "main": "medusa-config.js",
  "engines": {
    "node": ">=20.0.0"
  },
"scripts": {
    "build": "medusa build",
    "postbuild": "mkdir -p .medusa/server && cp package.json .medusa/server/ || true; cp -a dist/. .medusa/server/ 2>/dev/null || true; cp -a build/. .medusa/server/ 2>/dev/null || true",
    "start": "medusa start",
    "dev": "medusa dev",
    "db:migrate": "medusa db:migrate",
    "predeploy": "medusa db:migrate"
  },
          "dependencies": {
    "@medusajs/admin-sdk": "^2.5.1",
    "@medusajs/cli": "^2.5.1",
    "@medusajs/framework": "^2.5.1",
    "@medusajs/medusa": "^2.5.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}